"""벤치마크 통계 판정층 (meeting-tracker — 감지·STT 벤치 공유).

핵심 입장: **유효표본은 flag(아이템)가 아니라 회의(cluster)다.** 회의 안에 군집된
아이템은 독립이 아니므로, 모든 추론은 회의 수준에서만 정당하다. 이 층의 임무는
"얼마나 좋은가"를 판정하는 게 아니라 —

  (a) 극소 n에서 방어 불가능한 주장을 fail-loud로 **거부**하고,
  (b) 데이터 독립 공식으로 "얼마나 더 모아야 말할 수 있는가"를 **답**한다.

킬러 정직성 함수: min_attainable_two_sided_p(n)=2/2ⁿ, comparison_floor_n(α)=⌈log₂(2/α)⌉.
n=3이면 어떤 데이터가 나와도 쌍체 유의 최소 p=0.25>0.05라 구조적으로 유의 불가.

순수·결정적·런타임 의존성 0(stdlib만). 결정성(seed)은 재현성이지 통계적 정밀이 아니다 —
모든 산출 라벨이 이를 강제한다.
"""
